#ifndef VIBE_YAML_HELPERS_H
#define VIBE_YAML_HELPERS_H

/*
 * @file yaml_helpers.h
 * @brief Internal YAML helper declarations (C++ edition).
 *
 * Previously contained C buffer-handling utilities that have been superseded
 * by std::string / std::vector in the C++ parser modules.  This header is
 * retained for backward compatibility but no longer declares any functions.
 *
 * These utilities are NOT part of the public API — consumers should include
 * yaml_simple.h instead.
 */

#endif /* VIBE_YAML_HELPERS_H */
